#include "app.h"
#include "syscall.h"

void AMain()
{
    // uint32_t mutex = CreateMutex();

    printf("%s mutex = %p\n", __FUNCTION__, 0u);

    // EnterCritical(mutex);

    // ExitCritical(mutex);

    // DestroyMutex(mutex);
}
